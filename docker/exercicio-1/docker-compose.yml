version: '3.8'

services:
  app:
    build: .
    container_name: exercicio-1-nodejs
    ports:
      - "3000:3000"
    # Volume para desenvolvimento (opcional - permite edição em tempo real)
    # volumes:
    #   - .:/app
    #   - /app/node_modules
    restart: unless-stopped
